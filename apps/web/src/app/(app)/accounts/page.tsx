import { Placeholder } from '@/components/shell/placeholder';

export default function AccountsPage() {
  return (
    <Placeholder
      title="Accounts"
      note="List, create, edit, archive — and valuation snapshots — arrive in F2."
    />
  );
}
