import { Placeholder } from '@/components/shell/placeholder';

export default function TransactionsPage() {
  return (
    <Placeholder
      title="Transactions"
      note="Quick-add, filtered list, and the transfer form arrive in F3."
    />
  );
}
