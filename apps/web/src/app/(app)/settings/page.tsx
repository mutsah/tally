import { Placeholder } from '@/components/shell/placeholder';

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      note="Account settings come later in the build."
    />
  );
}
