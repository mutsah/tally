# The one switchable endpoint config. Base URL of the Nest API the BFF proxies
# to (includes the api/v1 global prefix). Server-side only — not exposed to the
# browser (the browser talks to same-origin Next route handlers).
API_BASE=http://localhost:3002/api/v1
